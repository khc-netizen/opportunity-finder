name: Apply Discovery Quality Pass

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  quality-pass:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled